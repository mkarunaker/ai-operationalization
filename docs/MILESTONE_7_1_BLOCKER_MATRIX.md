# Milestone 7.1 — Authoring and visual-revision hardening matrix

Status: the earlier blockers, latest implementation findings, follow-up evidence gaps, and the newest visual-version findings below are remediated. Their strengthened direct regressions and the complete no-provider local gate pass. The additive migrations `020` through `023`, this matrix, and the audit prompt are staged and appear in `git diff HEAD` for the independent audit. This remains a post-Milestone-7 hardening milestone; it is not an approval claim and does not reopen the independently approved Milestone 7 visual-brief contract.

| Invariant | Implementation boundary | Direct regression and adversarial fixture | Relevant assertion |
| --- | --- | --- | --- |
| A generated output honors its immutable selected reader range or is truthfully non-successful | `assertReaderFacingGeneratedOutput()` at every initial and derived drafting save/recovery path | `grounded-editorial-run.test.ts` — `rejects an under-range generated draft without saving it, while a compliant exact range remains current` and `rejects an under-range derived short post in both the Board run and its scoped recovery`; long 120–130 and short 73–77 ranges with 25-word invalid, 124-word article, and 75-word short fixtures | Invalid initial or derived output creates a terminal/partial failed stage and no current output; compliant exact output is saved with its immutable contract. |
| Reader-facing prose never leaks capture, source, or prompt scaffolding | Exact-output draft validation plus Write editor projection | `grounded-editorial-run.test.ts` — `keeps capture and source scaffolding out of reader-facing deterministic draft prose`; `local-shell.spec.ts` — `never presents captured prompt or source scaffolding as reader-facing draft prose`; injection-shaped capture uses `The following themes` and `Ignore prior instructions` | Neither persisted reader-facing text nor the Write textarea contains capture fragments, BOK labels, injection wording, or grounding markers; separately labelled reader provenance remains available. |
| Every relevant drafting/review boundary gets the saved reader contract while reader notes remain untrusted | Main Board, scoped derived recovery, Initial Drafter recovery, targeted rerun, and proofreader request builders | `grounded-editorial-run.test.ts` — `uses adversarial reader notes and unmistakable ranges at every generic drafting and review boundary`, `uses the immutable saved Board reader contract for scoped estimates and recovery after Develop preferences change`, `uses the immutable saved Board reader contract in a targeted reviewer rerun`, and `uses the immutable saved Board reader contract for proofreader estimate and live execution`; 1234–1567 / 321–357 ranges and an escaped closing-context note | Exact system prompts contain saved audience/ranges but never the free-form note; every untrusted request boundary contains the bounded author note, and later mutable Develop preferences are absent. |
| An author can describe a visual without a mismatched diagram or fictional image | Bounded visual-intent schema, exact-output recommendation, renderer eligibility, and Write UI | `lean-service.test.ts` — `keeps an author-requested custom illustration as an unrenderable concept instead of forcing it into a diagram`; `local-shell.spec.ts` — `keeps a literal author visual direction as a local custom-illustration concept`; injection-shaped building metaphor fixture | Intent persists as an honest custom-illustration concept; no template, render/approve control, provider dispatch, or price is offered. |
| A proofread result explains its actual safe state and next allowed action | Exact-output proofread persistence, safe error projection, Write and Finalize UI | `lean-service.test.ts` — `records %s as a terminal, publication-ineligible proofread outcome`, `rejects a proofread cap before dispatch and leaves the live-required review ineligible`, and `drops a cosmetic no-op proofreader finding whose rationale says no correction is needed`; `local-shell.spec.ts` — `explains an unattempted live proofread and points to the exact allowed retry`, `shows a classified proofread failure with the exact Write retry`, and `explains each exact-output Finalize proofread blocker without enabling publish` | Provider failure, refusal, truncation, repair exhaustion, and cap rejection retain safe categories; no-op findings cannot block Finalize; browser text names the state and exact review retry while Finalize remains disabled. |
| Reviewer truncation never silently incurs another request and has a bounded, visible recovery | Reviewer lifecycle/telemetry, saved-run projection, central rerun route, and Board UI | `grounded-editorial-run.test.ts` — `records one Skeptic truncation, then links an explicit scoped recovery without replacing the Board history`; `board-status.test.ts` — `allows only an explicitly linked scoped reviewer recovery to resolve that reviewer failure`; `local-shell.spec.ts` — `shows a failed reviewer as a scoped recovery without replacing the saved Board` | One failed Skeptic dispatch, zero automatic retries, preserved original partial run, separately linked recovery, bounded medium-tier cost/cap action, and no Board/draft replacement. |
| A working-draft truncation can be retried without rerunning a completed Board | Saved Initial Drafter snapshot, immutable contract/synthesis/retrieval, server-owned route/cap, attempt history, and Board UI | `grounded-editorial-run.test.ts` — `preserves an Initial Drafter output-limit failure, then retries only that saved stage with its immutable Board contract`, `rejects an Initial Drafter retry cap before provider dispatch and leaves the failed Board terminal`, and `keeps a second Initial Drafter failure terminal and separately persisted`; `local-shell.spec.ts` — `shows an Initial Drafter output-limit as a scoped, costed recovery without a Board rerun` | One original failure plus one explicit recovery attempt; old attempt persists; retry uses the original 1234–1567 executive contract despite changed Develop preferences, no reviewer/synthesis call recurs, cap rejection occurs before dispatch, and a repeat failure is terminal. |
| An author can replace one lead visual without losing history; new supporting authoring remains deferred | Additive lead-revision migration/service, active-lead projection, visual storage, Write/Finalize UI | `lean-service.test.ts` — `keeps a rendered lead visual active while an author prepares, renders, and reselects a separate replacement version`; `local-shell.spec.ts` — `keeps lead visual versions immutable while retaining an independent derived-short lifecycle`; flow Version 1 and forest contrast Version 2 fixture | Versions and files remain distinct and downloadable; active-lead selection is explicit; older assets remain historical; no normal supporting-creation control appears. |
| The primary visual workflow is discoverable before deep review detail | Write layout and browser accessibility order | `local-shell.spec.ts` — `organizes Write as one clear setup, matched output editors, and flat review surfaces` plus visual lifecycle flows | The visual entry point appears adjacent to the exact saved output before the long review-result surface; exact-version locks remain unchanged. |

## Independent-audit remediation — 2026-08-11

| Original blocker | Implementation location | Direct regression and adversarial fixture | Assertion |
| --- | --- | --- | --- |
| Initial Drafter recovery could offer a third paid attempt | `src/editorial/grounded-run.ts` — recovery selector excludes an existing `recoveryKind: "retry"`; `src/editorial/live-run.ts`; `app/queue-client.tsx` | `grounded-editorial-run.test.ts` — `keeps a second Initial Drafter failure terminal and separately persisted`; `local-shell.spec.ts` — `removes the Initial Drafter retry control after its one permitted retry has failed` and `explains that Initial Drafter route drift requires a new Board run` | The third test-only call rejects before dispatch, exactly two Initial Drafter attempts remain, preview is unavailable, and the browser renders either one-retry exhaustion or configuration-drift guidance but no retry control. |
| Initial Drafter recovery could estimate/dispatch after route drift | `src/editorial/grounded-run.ts` — manifest `initialDrafterRoute` persists provider/model/tier/pricing/maxOutputTokens; shared preflight compares all five before estimate and production dispatch | `grounded-editorial-run.test.ts` — `rejects a changed saved Initial Drafter %s before the production retry can dispatch`; synthetic provider/model/tier/pricing/1801-token mutations | Each changed persisted field makes preview unavailable, estimate `undefined`, and production retry reject before a second call is recorded. |
| Derived drafting omitted saved capture from final reader-facing guard | `src/editorial/grounded-run.ts` — same-run and scoped-derived calls to `assertReaderFacingGeneratedOutput` | `grounded-editorial-run.test.ts` — `rejects an unaligned compliant-length Initial Drafter capture fragment containing one-character words before saving it`, `rejects an unaligned same-run derived short capture fragment containing one-character words even when its word range is compliant`, and `rejects an unaligned scoped derived-short recovery capture fragment containing one-character words rather than using mutable Develop text`; `alpha a bridge I …`.split(" ").slice(1, 13) plus compliant 180+ word bodies | Initial save is rejected; same-run output is partial with article retained but no derived short; scoped recovery rejects and saves no new relationship. |
| Proofread no-op filtering erased meaning-changing punctuation | `src/lean/service.ts` — case/spacing-only comparison retains punctuation | `lean-service.test.ts` — `retains a meaning-changing punctuation correction as material and blocks Finalize until disposition`; `Let's eat Grandma.` / `Let's eat, Grandma.` | Material finding persists, `publishIdea()` rejects until explicit disposition, then publication records normally. |
| Supporting visual creation remained reachable below the UI | `app/api/ideas/[ideaId]/route.ts`; `src/lean/service.ts` — recommend/edit/approve/render/dismiss restrictions | `lean-service.test.ts` — `rejects new supporting visual creation below the service boundary`, `rejects a new supporting visual through the real local route`, and `never projects a stored supporting-only asset as the lead visual`; synthetic legacy supporting row/asset | New route/service authoring is rejected with a sanitized explanation; legacy support remains readable and cannot be edited or rendered. |
| Dismissed custom illustration concepts disappeared from version history | `src/lean/service.ts` — `visualRevisionHistory`; `app/queue-client.tsx` — read-only custom-concept history | `lean-service.test.ts` — `retains a dismissed custom-illustration concept in visible version history without creating a renderable asset`; `local-shell.spec.ts` — `keeps a dismissed custom illustration revision visible as saved no-render history after reload`; building/foundation direction | Reloaded history retains author direction and dismissed state; its UI has no control, rendering cost, provider, or image action. |
| Migrations 020/021 were absent from the auditable Git patch | `migrations/020_visual_lead_revision_selection.sql`; `migrations/021_visual_version_color_schemes.sql`; `tests/integration/migrations.test.ts` | `npm run db:validate`; `migrations.test.ts` populated upgrade regression | Temporary-database validation loads all 21 additive migration files. The migrations are staged with the audit artifacts, so Sol can inspect the schema in `git diff HEAD`. |
| Initial Drafter retry could race after read-only eligibility but before attempt persistence | `migrations/022_initial_drafter_recovery_claim.sql`; `src/editorial/grounded-run.ts` — `claimInitialDrafterRecovery()` and recovery availability query | `grounded-editorial-run.test.ts` — `atomically claims the one Initial Drafter retry before dispatch when concurrent callers overlap`; latched 190-word provider with two overlapping test-only recovery calls | First request creates one unique claim only after no-dispatch budget preflight; second rejects before provider use; preview becomes unavailable while the first waits; release produces one recovery model call and no third opportunity. Cap rejection regression also asserts no claim. |
| Capture-fragment protection skipped copied text beginning between sampled offsets | `src/editorial/grounded-run.ts` — `capturedFragments()` examines every 12-word normalized window | `grounded-editorial-run.test.ts` — `rejects an unaligned compliant-length Initial Drafter capture fragment containing one-character words before saving it`, `rejects an unaligned same-run derived short capture fragment containing one-character words even when its word range is compliant`, and `rejects an unaligned scoped derived-short recovery capture fragment containing one-character words rather than using mutable Develop text`; `alpha a bridge I …`.slice(1, 13) fixture | Each valid-length hostile response is rejected; no initial output, same-run derived relationship, or scoped recovery relationship is saved. |
| Audited patch did not include the schema and handoff artifacts it depended on | `migrations/020_visual_lead_revision_selection.sql`; `migrations/021_visual_version_color_schemes.sql`; `migrations/022_initial_drafter_recovery_claim.sql`; `docs/MILESTONE_7_1_BLOCKER_MATRIX.md`; `docs/MILESTONE_7_1_SOL_AUDIT_PROMPT.md` | `migrations.test.ts` — `creates required tables and the FTS index` and `maps populated legacy output records without losing publication dependents`; current Git inventory review before resubmission | Fresh migration includes visual selection/palette and recovery-claim schemas; upgrade fixture removes 020–022 before applying all current additive migrations; all named artifacts are staged so `git diff HEAD` is schema-complete. |
| Capture guard dropped one-character normalized tokens and missed copied `a`/`I` fragments | `src/editorial/grounded-run.ts` — `capturedFragments()` retains all non-empty normalized tokens before every contiguous 12-word window | `grounded-editorial-run.test.ts` — `rejects an unaligned compliant-length Initial Drafter capture fragment containing one-character words before saving it`, `rejects an unaligned same-run derived short capture fragment containing one-character words even when its word range is compliant`, and `rejects an unaligned scoped derived-short recovery capture fragment containing one-character words rather than using mutable Develop text`; unaligned `alpha a bridge I …`.slice(1, 13) fixture | All three compliant-length paths reject the copied normalized fragment before an output or derived relationship is persisted. |

## Latest Sol audit remediation — 2026-08-11

| Sol blocker | Implementation boundary | Direct regression and adversarial fixture | Assertion |
| --- | --- | --- | --- |
| A saved voice could drift before Initial Drafter recovery | `src/editorial/grounded-run.ts` — shared saved-voice loader compares both the persisted voice-version checksum and freshly read synthetic source checksum with the saved Board checksum before availability, estimate, or dispatch | `grounded-editorial-run.test.ts` — `rejects an Initial Drafter recovery when the saved voice source has changed`; failed Board then changed synthetic voice fixture | Recovery availability is false, estimate is unavailable, and injected recovery rejects before any provider request. The safe explanation requires a new Board after indexing the configured source. |
| Mutable visual-brief edits could renumber immutable visual assets | Additive `migrations/023_visual_asset_version_sequence.sql`; `src/lean/service.ts` exposes immutable `revisionNumber` separately from mutable `briefRevisionNumber`; `app/queue-client.tsx` displays the asset sequence | `lean-service.test.ts` — lead replacement edits Version 2 palette and claim before approval; `local-shell.spec.ts` — `keeps lead visual versions immutable while retaining an independent derived-short lifecycle` | Editing Version 2 leaves it Version 2 of 2, while its mutable brief revision advances. Both Version 1 and Version 2 assets remain separately rendered and downloadable. |
| An unlisted literal illustration direction could inherit an unrelated output keyword diagram | `src/lean/service.ts` — positive `supportedVisualTemplateFromDirection()` mapping; unrecognized direction becomes an honest no-render custom concept | `lean-service.test.ts` — bridge connecting strategy and operations fixture against saved prose containing diagram-worthy `framework` and `path` terms | No template is selected and no approval/render action is available; the literal author direction remains saved as a custom-illustration concept. |
| Browser recovery copy called every recoverable Initial Drafter failure an output-limit failure | `src/editorial/grounded-run.ts` safe category projection; `src/lean/service.ts` whitelisted saved failure category; `app/queue-client.tsx` category-specific recovery wording | `local-shell.spec.ts` — reader-range and capture-scaffolding failure cases explicitly reject output-limit wording | Range and scaffolding failures describe their actual saved validation boundary and allowed new-Board action. Only `output_limit` receives output-limit wording. |

## Follow-up Sol evidence remediation — 2026-08-11

| Sol finding | Implementation boundary | Direct regression | Assertion |
| --- | --- | --- | --- |
| Version 2 claim editing was described but not directly exercised | `src/lean/service.ts` keeps `visual_version_number` immutable and increments only `revision_number`; `app/queue-client.tsx` submits the author’s changed claim | `lean-service.test.ts` — Version 2 changes its traceable claim to `measurable outcomes`, label to `accountable ownership`, and palette to forest; `local-shell.spec.ts` changes the Version 2 claim to `A clear owner` through the browser editor | Service and browser both retain `revisionNumber: 2`, advance `briefRevisionNumber: 2`, persist the changed claim and forest palette, display Version 2 of 2, and retain distinct V1/V2 assets. |
| Browser scaffolding wording was tested only with a supplied category | `src/editorial/grounded-run.ts` classifies the safe persisted scaffolding summary; `src/lean/service.ts` reads the saved model-call category; `app/queue-client.tsx` renders it | `grounded-editorial-run.test.ts` — real hostile Initial Drafter output asserts persisted `reader_prose_scaffolding_failed`; `local-shell.spec.ts` — `projects a persisted Initial Drafter scaffolding failure into truthful browser guidance` creates the real failure through the deterministic local fixture and mocks only retry availability | The browser reads the category from the persisted API projection, shows scaffolding guidance, and rejects output-limit wording. No browser fixture supplies the category. |

## Latest Sol visual-version remediation — 2026-08-11

| Sol blocker | Implementation boundary | Direct regression | Assertion |
| --- | --- | --- | --- |
| Same-millisecond V1/V2 render could overwrite one asset file | `src/lean/service.ts` binds every newly created file name to draft version, immutable visual version, and brief-ID suffix; refresh preserves an existing path | `lean-service.test.ts` — freezes the clock while rendering V1 and V2 | V1/V2 IDs and paths differ even at identical timestamps; both files exist; V1 SVG is byte-for-byte unchanged after V2 renders. |
| A dismissed custom concept made next/total browser version numbers inconsistent | `app/queue-client.tsx` derives next and total labels from complete immutable `visualRevisionHistory`, while arrows still traverse rendered assets | `local-shell.spec.ts` — rendered V1 → dismissed custom concept V2 → rendered diagram V3 | The preparation disclosure predicts visual version 3; candidate is Version 3; rendered navigation says Version 3 of 3. |
| Initial custom concept followed by a supported alternative could create duplicate Version 1 records | `src/lean/service.ts` atomically dismisses a pending literal custom concept before allocating the next `visual_version_number`; a generic no-visual recommendation is instead promoted in place | `lean-service.test.ts` — bridge concept V1 then explicit decision-fork alternative; `local-shell.spec.ts` — `keeps a literal author visual direction as a local custom-illustration concept` continues through the alternative, approval, and render | Literal V1 becomes intentionally dismissed; supported diagram is immutable V2; history is uniquely `[1 dismissed concept, 2 rendered diagram]`; browser displays Version 2 of 2. |

No provider call, meaningful-data migration, private-source access, staging, commit, or push is authorized by this matrix. The validation record is in `IMPLEMENTATION_STATUS.md`. Sol independently approved Milestone 7.1 on 2026-08-11 after a final read-only audit with no blockers.
