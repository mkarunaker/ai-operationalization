# Milestone 7 — fourth-remediation independent Sol read-only audit record

Sol independently approved the Milestone 7 deterministic visual-brief fourth remediation on 2026-08-10. This retained prompt records the audit scope and direct evidence; it is not a substitute for a future independent audit if the implementation changes.

Read in full: `AGENTS.md`, `BUILD_ROADMAP.md`, `LEAN_PRODUCT_SCOPE.md`, `IMPLEMENTATION_STATUS.md`, `docs/AGENT_SECURITY_GUARDRAILS.md`, `docs/MILESTONE_7_BLOCKER_MATRIX.md`, and the complete current diff.

Do not run migrations, call providers, read `.env` files, secrets, databases, BOK, voice, Notebook, private content, or provider request bodies. Do not stage, commit, or push.

Verify directly:

1. A saved output may receive `no_visual`; the template controls start unselected, and selection is an explicit author request.
2. A visual brief records an exact saved draft and reader/output snapshot; later output saves cannot reuse it.
3. Author direction, claims, labels, caption, and alt text are bounded; claims must be literal, traceable content from the saved output; hostile display text remains escaped/plain text.
4. Render rejects every unapproved/no-visual/wrong-output brief. Approval is explicit, and a refresh can use only the same rendered exact brief.
5. The real `update_visual_brief` route strips only its action envelope before strict validation; extra author payload keys still fail. Its recognized visual lifecycle errors remain actionable while arbitrary errors remain sanitized.
6. Rendering and refresh derive their grammar and all variable text from the approved exact brief only. A caller cannot supply a template at the render route or service boundary. The saved SVG, its data-URL preview, and PNG export use approved labels/claims/direction; SVG `aria-label` uses approved alt text and `<desc>` uses the approved caption.
7. One exact output permits one lead plus two supporting briefs/assets, rejects a second lead/third support both on creation and on an attempted placement edit, and returns supporting assets independently. The Write view has author-facing approve/render/edit paths for lead and supporting briefs.
8. A derived short post has an independently selected exact-output brief, asset, approval, reader-contract provenance, and Write lifecycle. It must never be silently substituted with the canonical article.
9. `vertical_path` is the canonical author/persistence identifier; legacy renderer `maturity_path` remains readable only as a normalized compatibility value.
10. The saved visual reader contract remains immutable after Develop preferences change; no rendering path rereads mutable preferences or current output text.
11. The stored SVG remains under the approved configured visual root, with title/version/timestamp naming and owner-only permissions.
12. Deterministic rendering presents `$0.00 local` before approval. No image provider is configured or dispatched. The documented future `gpt-image-2` route is not an implemented provider capability and must not be treated as one.
13. Existing reader-first exact-version, Finalize, safe-error, SVG escaping/text-containment, and publication safeguards remain intact.
14. A derived-short `no_visual` recommendation has its own unselected four-shape picker. Selecting a derived-only shape requests a new visual recommendation; it never offers an impossible approval for the `no_visual` brief and never uses the article selector state. Supporting, derived-short, and lead approval/render sections each disclose the deterministic `$0.00 local` cost beside their action; `no_visual` has no approval/render action to price.
15. Lead/support cardinality is atomic: `recommendVisualBrief()` takes a writer transaction, and migration 019 enforces one active lead plus at most two active supports using database-level constraints/triggers. Inspect direct-insert and placement/status-update behavior; a service pre-check alone is insufficient.
16. Publication locks are exact-output scoped for visual recommendation, brief edits, and rendering. After an article publication record, a current unpublished derived short remains independently selectable, approvable, and renderable; once that exact derived short is published, its own visual operations reject.
17. Every deterministic re-render action displays `$0.00 local` in the same action row as Refresh and Download, including the lead, each support, and the independently rendered derived short. A cost disclosure elsewhere on the page is insufficient.
18. Supporting identity is never promoted to lead. The route/service must reject a supporting recommendation without an active exact-output lead; a supporting asset must not render until that lead’s own asset is rendered; and `getIdea()` must expose a stored supporting-only record only in the supporting collection, never as `visualCompanion` or `visualBrief`.
19. `vertical_path` is the only persisted vertical visual-brief grammar. Migration 019 and direct SQL insertion/update must reject `maturity_path`; the renderer may normalize that legacy value only for pre-existing non-brief visual asset records.
20. Migration 019 must retain pre-brief visual rows with their intentionally null optional `visual_brief_id`. `getIdea()` may show such an actual legacy asset as the primary read-only visual in Write and Finalize, but may never use a modern supporting asset as that fallback, let a legacy asset satisfy a lead brief, or offer an unsupported refresh.

Direct evidence includes `local visual asset storage > accepts a strict visual-brief edit through the real API action envelope`, `renders only approved brief content and never lets a render request replace its approved template`, `rejects a caller-supplied template at the real approved-render route`, `keeps placement limits coherent when a visual brief is edited`, `enforces lead and supporting limits in the database even when a caller bypasses the service count`, `allows an unpublished derived-short visual lifecycle after its article has been recorded`, `uses vertical_path consistently from edit, persistence, reload, and rendering`, `targets a derived short post independently and exposes its own visual brief`, `preserves the saved reader contract after later Develop preference changes`, `requires a lead visual brief before a supporting brief can be requested`, `requires the rendered lead asset before a supporting asset can render`, `never projects a stored supporting-only asset as the lead visual`, `rejects the legacy maturity_path value in visual-brief persistence`, and `retains a pre-brief visual through the real detail route for Write and Finalize`; plus migration `maps populated legacy output records without losing publication dependents` and Playwright `keeps approved visual grammar immutable while exposing article support and derived-short visual lifecycles`, `lets an author replace a derived-short no-visual recommendation with its own selected shape`, `keeps a saved derived short post independently editable and reviewable after article publication`, and `keeps a legacy unlinked visual readable in Write and Finalize`.
