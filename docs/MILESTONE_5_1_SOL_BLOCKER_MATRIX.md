# Milestone 5.1 — Sol blocker-to-regression matrix

Status: approved. The three newly reported blockers have direct adversarial regressions, the complete local validation gate passed on 2026-08-09, and an independent Sol read-only audit found no remaining blockers.

| Blocker invariant | Implementation boundary | Direct regression and adversarial fixture | Expected failure before remediation |
|---|---|---|---|
| Immutable reader contract is a strict, minimal provenance object | `src/editorial/grounded-run.ts` `sourceManifest()`; `src/lean/service.ts` provenance loader | Add a grounding regression with non-empty capture, notes, answers, themes, IDs, and plan; assert strict equality of the stored and returned contract; inject an extra manifest key and assert provenance rejects it. | Full `SnapshotInput` is serialized and loader re-exposes extra fields |
| Proofreader disclosure is selected-output-specific and honest about unavailable routing | `src/editorial/live-run.ts` preview; `app/queue-client.tsx` review controls | Add dual-output preview/browser regressions with deliberately different canonical and companion sizes; assert each button uses its own exact-output two-attempt estimate, and unavailable proofreader renders deterministic/no-provider wording without a model/cost claim. | One canonical estimate appears on both buttons and unavailable routing is labelled live proofread |
| Exported production proofreader boundary rejects arbitrary routing | `src/lean/service.ts` exported proofreader entry point | Add a direct no-network call with injected provider/model/tier/pricing/cap and assert rejection before dispatch; retain a clearly test-only injection seam for outcome matrix coverage. | Direct exported execution accepts caller-selected route/pricing outside HTTP |
| Persisted telemetry records attempted route identity separately from response identity | `src/editorial/grounded-run.ts` `persistModelCall()` | Strengthen malformed-repair regression: request is `openai/test-low`, response self-identifies differently; assert database provider/model are attempted route and raw usage retains response identity. | Database columns silently adopt response-reported route identity |
| Production proofreader dispatch cannot be adapter-injected even when metadata matches the configured route | `src/lean/service.ts` `serverResolvedProofreaderInput()` and `ServerResolvedProofreaderProvider` | `tests/integration/lean-service.test.ts` — “ignores a matching-route injected adapter at the exported production proofreader boundary”: malicious `ProofreadOutcomeProvider` plus exact configured metadata and a valid cap; external transport is disabled, server-owned dispatch fails safely, and the malicious provider has zero calls. “rejects an excessive proofreader cap independently before any matching-route adapter dispatch” proves the cap branch separately. | The supplied adapter dispatches successfully because metadata equality alone passes. |
| Immutable reader-contract provenance has coherent ranges and at least one selected output | `src/lean/service.ts` `provenanceReaderContract` | `tests/integration/grounded-editorial-run.test.ts` — “rejects reader-contract prompt manifests with extra fields, incoherent ranges, or no selected output”: a tampered manifest has an extra field, `longForm.min` 10,000 / `max` 100, then neither output range; each must yield no rendered provenance. | Individually bounded but incoherent/no-output contracts are rendered as provenance. |
| Response identity cannot overwrite either part of the attempted model route | `src/editorial/grounded-run.ts` `persistModelCall()` | `tests/integration/lean-service.test.ts` — “persists malformed and repaired live-proofread attempts separately before making findings eligible”: attempted route remains `openai/test-low` while both provider responses self-identify as `injected-proofreader/response-claimed-model`; database columns must retain the attempted route and raw usage must retain the response identity. | Response-reported provider or model replaces the attempted route in persisted columns. |

Every regression must be made directly failing before its corresponding implementation change and must be recorded in the final handoff with its exact test name and assertion.
