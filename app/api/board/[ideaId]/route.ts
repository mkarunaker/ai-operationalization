import { decideRecommendation, runBoard } from "@/board/service";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function POST(request: Request, context: RouteContext<"/api/board/[ideaId]">) { try { const { ideaId } = await context.params; const body = await request.json() as { action?: string; budgetCap?: number; recommendationId?: string; decision?: "accepted" | "partially_accepted" | "rejected"; note?: string }; if (body.action === "decide" && body.recommendationId && body.decision) { decideRecommendation(body.recommendationId, body.decision, body.note); return Response.json({ ok: true }); } return Response.json(await runBoard(ideaId, Math.max(0, Number(body.budgetCap) || 0))); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Unable to run the editorial board." }, { status: 400 }); } }
