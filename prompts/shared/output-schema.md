# Structured Output

Return the required JSON envelope. If a field cannot be supported, say so plainly rather than inventing content.

Keep every string concise. Return no more than three findings, strengths, risks, recommendations, factual gaps, or voice rules. Prefer fewer material items over exhaustive lists.
