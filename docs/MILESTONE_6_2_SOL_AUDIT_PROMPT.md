# Milestone 6.2 post-approval UX closure — independent Sol audit instructions (completed and approved 2026-08-10)

Conduct a new independent **read-only** audit of the bounded Milestone 6.2 post-approval UX closure. Return an explicit `APPROVE` or `DO NOT APPROVE` verdict for this closure. The previously approved reader-first contract is context; re-evaluate it only where this current diff could have regressed it. An approval must be based on direct source-and-regression evidence, not the aggregate green test count.

Read, in full:

- `AGENTS.md`
- `BUILD_ROADMAP.md`
- `LEAN_PRODUCT_SCOPE.md`
- `AI_Editorial_Board_Spec.md`
- `IMPLEMENTATION_STATUS.md`
- `docs/MILESTONE_6_2_BLOCKER_MATRIX.md`
- `docs/AGENT_SECURITY_GUARDRAILS.md`
- `AUDIT_2026-08-07.md`
- the complete current diff

Do not modify files, apply migrations, run providers, access `.env` files, secrets, BOK, voice, private sources, or database contents, and do not stage, commit, or push. Do not run suites if their configuration applies migrations, even to temporary databases; inspect the direct regression source instead.

Audit the Milestone 6.2 contract plus the bounded post-approval UX closure recorded in `BUILD_ROADMAP.md`, and these boundaries:

1. From Capture through Write, active UI, service, route, model prompts, saved formats, relationships, and tests use only reader/output concepts: short post, article, and derived short post. Historical migrations and compatibility mapping may mention legacy delivery platforms. A `channel` may occur only when recording an exact approved output in Finalize.
2. Migration 018 is additive and preserves populated legacy synthetic records and dependent publication history while mapping legacy output plans, formats, relationships, approvals, reader context, and publication delivery data to the new generic schema.
3. The immutable saved reader contract—including adversarial reader notes and unmistakably non-default ranges—reaches every relevant reviewer, canonical drafter, initial derived-short drafter, scoped estimation, and scoped recovery boundary. Reader notes remain escaped, bounded, untrusted context and never trusted system instructions.
4. Exact-version behavior is preserved: derived short posts link to exact article versions, review/proofread and stale-state rules remain independent, unchanged saves do not create fake versions, and the generic Finalize flow enforces sequencing and server-side locks.
5. Live-proofread boundaries remain server-routed, capped before dispatch, bounded to one same-route repair, terminal on non-success, telemetry-complete per attempt, and ineligible after missing, failed, or unattempted live-required proofread. Verify this against direct adversarial tests as well as code.
6. Browser regressions prove reader/output-only authoring, immutable provenance distinct from mutable preferences, Finalize-only channel selection, generic output lifecycle, provider-route isolation, and truthful unavailable/failed/missing/material-review states.
7. Confirm local mutation errors are sanitized while known user-facing proofread and Finalize explanations remain actionable.
8. Inspect the UX closure directly: capture-title hierarchy, a thought-capture editor aligned to Save, and compact theme addition; confirmed unpublished-only queue deletion; compact original capture; compact Park/delete placement; bounded ordinary audience-note input; visibly separated reader/output sections with output shape and only active ranges aligned on wide screens; a stacked-radio—not card-like—evidence chooser with a distinct question group and evidence/interpretation/source group; compact saved Board status; matched non-overflowing Write editor cards with their review action beside the correct output and its model/cost disclosure outside the action label; short-only work must say `Short post review` and `exact short-post version`, never article; shared Knowledge-sources navigation; wider shell; and the local run ledger. Confirm all of these preserve reader-first language and exact-version workflow behavior.
9. For the local run ledger, verify the service aggregates exactly and only persisted attempt count, token total, and estimated cost for the current idea; queue and detail results agree; an actual `grounded-test` deterministic Board displays `$0.00 local`, never an estimated-cost label; cost is explicitly labelled an estimate, not an invoice; and no prompt, source, provider body, provider-classification field, raw error, credential, BOK, or voice text is returned or rendered. Verify the queue deletion action remains absent for published work and relies on the existing server-side published-history guard.
10. Audit the bounded visual and Finalize polish: the page preview must be the same escaped deterministic SVG representation used for export, without a competing HTML/CSS diagram; generated SVG files must resolve only beneath the configured ignored `visuals/` root, in readable paths with a title prefix capped at 20 characters plus a collision-resistant idea-ID suffix, never beside SQLite application data; the four visual-shape choices must share one desktop row and wrap only at the documented responsive breakpoints; every template must constrain headings and copy to explicit panels/line limits, and decision branches must end at their outcome panels. Character-count wrapping alone is insufficient: inspect the wide-glyph regression and verify every dynamic line receives a physical SVG `textLength` no wider than its named region, with `lengthAdjust="spacingAndGlyphs"`. The user-facing download must be PNG; Refresh and Download must be adjacent; the Finalize visual must use its smaller documented scale; and the exact-preview asset must not execute user/model content. Return to Write must look and behave as a clear navigation control. Disabled publication guidance may be concise, but audit that each current review, proofread, material-finding, and article-first server gate remains visible and enforced. Article and derived-short publication records must use the same layout and show only the exact output’s locally recorded delivery channel, URL or explicit absence, and date. Confirm these values are dynamic persisted publication data, not inferred distribution guidance.

Give specific attention to the remediated findings: change Develop preferences after a saved paired Board run and verify scoped estimate/recovery use only the strict saved manifest contract; inspect a targeted reviewer rerun for the same trusted audience, output shape, and ranges while its reader note remains untrusted; verify a missing BOK/voice index returns a truthful preview/UI state rather than an exception that removes the Board setup; then inspect the saved-manual-draft/no-Board case, all six legacy plan mappings on ideas and snapshots, strict mixed-payload rejection, and recording an article and derived short post on deliberately different channels.

For each concrete concern, cite its source path and line, explain the violated invariant, and identify whether a direct regression detects it. Do not ask for scope expansion or implementation preferences. Report any remaining limitation separately from a blocker.
